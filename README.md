# bin

+ `generic` :: theoretically useful on any modern Linux machine[⏎](./generic)
+ `specific` :: require more specific additional configuration[⏎](./specific)
+ `sunset` :: older scripts, kept for posterity[⏎](./sunset)

## Deployment

``` shell
# Link scripts
just
# Unlink scripts
just mode=delete
```
